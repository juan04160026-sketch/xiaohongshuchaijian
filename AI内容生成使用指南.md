# AI 内容生成使用指南

## 功能概述

本工具支持使用 Google Gemini AI 自动生成小红书笔记的标题、正文和封面图，大幅提升内容创作效率。

### 核心功能
- ✅ AI 生成小红书标题（20字以内，爆款格式）
- ✅ AI 生成小红书正文（200字以内，包含emoji和标签）
- ✅ AI 生成封面图片（1:1方形，清新风格）
- ✅ 自动上传到飞书多维表格
- ✅ 支持自定义提示词模板

---

## 前置准备

### 1. 获取 Gemini API Key

1. 访问 [Google AI Studio](https://aistudio.google.com/)
2. 登录 Google 账号
3. 点击 "Get API Key" 创建新的 API Key
4. 复制 API Key 备用

### 2. 配置代理（必需）

Gemini API 需要通过代理访问：

1. 安装并启动 **Clash Verge** 或其他代理工具
2. 确保代理端口为 **7897**（混合代理端口）
3. 测试代理是否正常：
   ```bash
   node test-proxy-connection.js
   ```

### 3. 飞书表格准备

确保飞书多维表格包含以下字段：
- **主题**（文本）：AI 生成的输入主题
- **小红书标题**（文本）：AI 生成的标题
- **小红书文案**（文本）：AI 生成的正文
- **小红书封面**（附件）：AI 生成的图片
- **状态**（单选）：待生成、已生成、待发布、已发布

---

## 配置步骤

### 步骤1：打开系统设置

在软件界面点击 **"系统设置"** 标签

### 步骤2：配置 AI 内容生成

找到 **"AI 配置 (Gemini)"** 部分：

1. **Gemini API Key**
   - 粘贴从 Google AI Studio 获取的 API Key
   - 点击 "测试文案连接" 验证配置

2. **文案生成模型**
   - 推荐：`Gemini 3 Flash Preview (最新)`
   - 或选择其他文本模型

3. **图片生成模型**
   - 默认：`gemini-3-pro-image-preview`
   - 这是目前唯一可用的图片生成模型

4. **测试连接**
   - 点击 "测试文案连接" 测试文本生成
   - 点击 "测试图片连接" 测试图片生成
   - 确保两个都显示 "✅ 连接成功"

### 步骤3：自定义提示词（可选）

如果需要自定义 AI 生成的风格，可以修改提示词模板：

#### 标题提示词模板
```
根据主题"{{主题}}"，创作一个小红书爆款标题。

要求：20字以内，数字+关键词或提问式，可用emoji。

只输出标题：
```

#### 正文提示词模板
```
根据主题"{{主题}}"，创作小红书笔记正文。

要求：
- 200字以内
- 大量使用emoji（✨💡🔥👉）
- 用❶❷❸分点
- 语气亲切，用"姐妹们"等网络热词
- 末尾5-8个标签（#标签）

只输出正文：
```

#### 图片提示词模板
```
A bright, clean, modern social media cover image about {{主题}}, pastel colors, 1:1 square, minimalist style, no text, no logos
```

**提示词优化技巧：**
- 使用 `{{主题}}` 作为占位符，系统会自动替换
- 明确要求 "直接输出"、"只输出"，避免 AI 添加解释
- 不要使用 `[此处填写...]` 这样的占位符
- 图片提示词建议用英文，AI 支持更好

### 步骤4：保存配置

点击 **"保存配置"** 按钮

---

## 使用流程

### 方式1：批量生成

1. **准备飞书数据**
   - 在飞书表格中填写 "主题" 字段
   - 将 "状态" 设置为 "待生成"

2. **启动生成**
   - 切换到 "图文合成" 标签
   - 点击 "批量生成内容" 按钮

3. **等待完成**
   - 系统会自动处理所有 "待生成" 的记录
   - 每条记录间隔 2 秒，避免 API 限流
   - 查看控制台日志了解进度

4. **查看结果**
   - 生成完成后，飞书表格会自动更新：
     - 小红书标题
     - 小红书文案
     - 小红书封面
     - 状态改为 "已生成"

### 方式2：单条生成

1. 在飞书表格中选择一条记录
2. 确保有 "主题" 且状态为 "待生成"
3. 点击 "批量生成内容"（会只处理这一条）

---

## 生成示例

### 输入
- **主题**：公众号运营

### 输出

**标题**（20字以内）：
```
揭秘！公众号从0到1月入过万💰
```

**正文**（200字以内）：
```
姐妹们！你的公众号是不是也进入瓶颈期了？🤔别慌！今天手把手教你激活它！✨

❶明确定位：你是谁？给谁看？想解决什么问题？💡 定位不清，一切白搭！

❷选题宝典：热点+痛点+爆点 🔥 用户关心什么就写什么！

❸标题杀手锏：数字+悬念+利益点 👉 让人忍不住点开！

❹内容为王：干货+故事+互动 💪 让读者看完就想转发！

❺持续输出：每周至少3篇 📅 坚持才能看到效果！

#公众号运营 #涨粉技巧 #自媒体 #内容创作 #变现攻略
```

**封面图**：
- 清新明亮的社交媒体风格
- 马卡龙色系
- 1:1 方形构图
- 自动保存并上传到飞书

---

## 常见问题

### Q1: 图片生成失败怎么办？

**症状**：控制台显示 "⚠️ 图片生成失败"

**解决方案**：
1. 检查代理是否正常运行（Clash Verge）
2. 确认 API Key 有图片生成权限
3. 尝试重新生成
4. 如果持续失败，可以：
   - 在飞书表格中手动上传图片
   - 或使用小红书的 "文字配图" 功能

### Q2: 标题或正文包含提示词本身？

**原因**：提示词格式不当

**解决方案**：
1. 检查提示词中是否有 `[此处填写...]` 这样的占位符
2. 改为 "直接输出标题" 或 "只输出正文"
3. 简化提示词，越简单越好

### Q3: 生成的内容不符合要求？

**解决方案**：
1. 修改提示词模板，明确要求
2. 调整字数限制、风格描述
3. 尝试不同的模型
4. 多生成几次，选择最好的

### Q4: API 请求失败？

**可能原因**：
- 代理未启动或端口错误
- API Key 无效或配额用完
- 网络连接问题

**解决方案**：
1. 确认 Clash Verge 正在运行
2. 测试代理：`node test-proxy-connection.js`
3. 检查 API Key 是否正确
4. 查看 Google AI Studio 的配额使用情况

### Q5: 图片上传飞书失败？

**症状**：图片生成成功但飞书表格中没有

**解决方案**：
- 图片已保存在本地：`C:\Users\你的用户名\.xhs-publisher\ai-images\`
- 可以手动上传到飞书表格
- 或者重新生成一次

---

## 技术说明

### 使用的 AI 模型

**文本生成**：
- `gemini-3-flash-preview`（推荐）
- `gemini-2.5-flash`
- `gemini-1.5-flash`
- `gemini-1.5-pro`

**图片生成**：
- `gemini-3-pro-image-preview`（唯一可用）

### 生成流程

1. **读取飞书数据**：获取所有状态为 "待生成" 的记录
2. **生成标题**：调用 Gemini API 生成标题
3. **生成正文**：调用 Gemini API 生成正文
4. **生成图片**：调用 Gemini API 生成图片
5. **保存图片**：保存到本地 `.xhs-publisher/ai-images/` 目录
6. **上传飞书**：上传图片到飞书并获取 file_token
7. **更新记录**：将标题、正文、图片写回飞书表格
8. **更新状态**：将状态改为 "已生成"

### 性能优化

- 批量生成时，每条记录间隔 2 秒
- 失败自动重试 3 次
- 支持断点续传（已生成的不会重复）

---

## 测试脚本

### 测试代理连接
```bash
node test-proxy-connection.js
```

### 测试标题生成
```bash
set GEMINI_API_KEY=你的key
node test-new-prompt.js
```

### 测试正文生成
```bash
set GEMINI_API_KEY=你的key
node test-content-generation.js
```

### 测试图片生成
```bash
set GEMINI_API_KEY=你的key
node test-simple-image.js
```

### 诊断图片生成问题
```bash
set GEMINI_API_KEY=你的key
node diagnose-image.js
```

---

## 最佳实践

### 1. 主题设置
- 主题要具体明确，如 "公众号运营" 而不是 "运营"
- 可以包含关键词，如 "公众号涨粉技巧"
- 避免太宽泛的主题

### 2. 批量生成
- 建议每次不超过 10 条记录
- 大批量生成可能触发 API 限流
- 可以分批次进行

### 3. 提示词优化
- 保持简洁明确
- 明确输出格式要求
- 避免复杂的嵌套指令

### 4. 图片生成
- 图片提示词用英文效果更好
- 描述要简单直接
- 避免要求生成文字（AI 图片生成不擅长文字）

---

## 费用说明

### Gemini API 定价

- **免费额度**：每分钟 15 次请求
- **付费版本**：根据使用量计费
- 详见：[Google AI Pricing](https://ai.google.dev/pricing)

### 成本估算

每条记录生成成本：
- 标题生成：约 0.001 美元
- 正文生成：约 0.002 美元
- 图片生成：约 0.01 美元
- **总计**：约 0.013 美元/条

100 条记录约 1.3 美元

---

## 技术支持

如遇问题：
1. 查看控制台日志
2. 运行相应的测试脚本
3. 检查本文档的常见问题部分
4. 确保代理和 API Key 配置正确

---

## 更新日志

### v1.0.0 (2025-01-13)
- ✅ 支持 AI 生成标题、正文、图片
- ✅ 自动上传到飞书多维表格
- ✅ 支持自定义提示词模板
- ✅ 支持批量生成
- ✅ 完善的错误处理和重试机制
