# 图片生成问题解决方案

## 问题：AI 图片不显示

### 快速诊断

运行诊断脚本：
```bash
set GEMINI_API_KEY=你的key
node diagnose-image.js
```

这个脚本会测试所有可用的图片生成模型，找出哪个可以用。

---

## 常见原因和解决方案

### 1. API Key 没有图片生成权限

**症状：** 返回 403 或 permission denied 错误

**解决方案：**
- Gemini 的图片生成功能可能需要特殊权限或付费账号
- 访问 [Google AI Studio](https://aistudio.google.com/) 检查你的账号权限
- 尝试申请图片生成 API 的访问权限

### 2. 模型不可用

**症状：** 返回 404 或 model not found 错误

**解决方案：**
- `gemini-3-pro-image-preview` 可能是预览版，不是所有账号都能用
- 尝试其他模型：
  - `imagen-3.0-generate-001`
  - `imagen-3.0-fast-generate-001`
- 在软件的"系统设置"中切换图片生成模型

### 3. 代理连接问题

**症状：** ECONNREFUSED 或 timeout 错误

**解决方案：**
- 确保 Clash Verge 正在运行
- 检查代理端口是否为 7897
- 测试代理：`node test-proxy-connection.js`

### 4. 提示词格式问题

**症状：** 返回文本而不是图片

**解决方案：**
- 使用英文提示词（AI 图片生成对英文支持更好）
- 提示词要简洁明确，直接描述图片内容
- 不要让 AI "生成提示词"，而是直接描述图片

---

## 备用方案

### 方案 A：不使用 AI 生成图片

1. 在飞书表格中手动上传图片到"小红书封面"字段
2. 软件会自动下载并使用这些图片
3. 标题和文案仍然由 AI 生成

### 方案 B：使用小红书的文字配图

1. 在软件设置中选择"文字配图"作为图片来源
2. 发布时小红书会根据文案自动生成封面
3. 不需要 AI 图片生成

### 方案 C：使用本地图片

1. 准备一批图片放在本地文件夹
2. 在软件设置中配置"本地合成图片"
3. 软件会随机选择图片作为封面

---

## 推荐配置

### 如果图片生成不可用

在"系统设置"中：
1. **图片来源** 选择：`飞书图片` 或 `文字配图`
2. **AI 内容生成** 只勾选：
   - ✅ 生成标题
   - ✅ 生成文案
   - ❌ 生成图片（取消勾选）

这样标题和文案由 AI 生成，图片用其他方式解决。

---

## 测试步骤

### 1. 测试代理连接
```bash
node test-proxy-connection.js
```

### 2. 测试文本生成
```bash
node test-new-prompt.js
node test-content-generation.js
```

### 3. 诊断图片生成
```bash
node diagnose-image.js
```

### 4. 如果找到可用模型
在软件的"系统设置" → "AI 内容生成" 中：
- 图片生成模型选择"自定义"
- 填写可用的模型 ID（如 `imagen-3.0-generate-001`）

---

## 技术说明

Gemini 的图片生成功能：
- 是较新的功能，可能不是所有账号都能用
- 需要在 API 请求中指定 `responseModalities: ['TEXT', 'IMAGE']`
- 返回的图片数据在 `inlineData` 字段中

如果你的 API Key 不支持图片生成，这是正常的。使用备用方案即可。

---

## 联系支持

如果以上方案都不行：
1. 检查 Google AI Studio 中是否有图片生成功能
2. 查看 Gemini API 文档了解最新的图片生成模型
3. 考虑使用其他图片生成服务（如 DALL-E、Midjourney 等）
